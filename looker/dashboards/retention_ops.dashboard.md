# Retention Ops Dashboard

- **Intervention shortlist:** `retention_decision_engine` where `selected_for_intervention = yes`, sorted by `roi_score` or `priority_rank`
- Customer table sorted by `priority_score` / `expected_mrr_saved` (from decision engine explore)
- Filters: segment, region, plan tier, `snapshot_date`
- Tiles: primary risk driver mix, recommended / policy action mix, sum **expected MRR saved** for selected cohort
- Link to **What-if** explore for capacity/threshold planning
