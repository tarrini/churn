# What-if Simulator dashboard

- Net expected value by `scenario_id` (bar)
- Selected customers by capacity and risk threshold (table)
- Expected MRR saved vs total action cost (stacked column)
- Marginal net value when increasing capacity (from `retention_capacity_marginal_gain`)
- Filter: snapshot date, risk threshold, capacity

Built on explores: `retention_what_if`, `retention_marginal_gain`.
